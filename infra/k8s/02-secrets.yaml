# TEMPLATE ONLY - do NOT commit real values. This file is gitignored.
#
# In production: values are injected from GCP Secret Manager via
# the External Secrets Operator (https://external-secrets.io/).
#
# For local K3s testing: base64-encode your values and fill them in.
#   echo -n "your-value" | base64
#
# All values below are base64-encoded placeholders.
# Replace before running `kubectl apply`.
apiVersion: v1
kind: Secret
metadata:
  name: nordspot-secrets
  namespace: nordspot
type: Opaque
data:
  # echo -n "nordspot" | base64
  CLICKHOUSE_USER: bm9yZHNwb3Q=
  CLICKHOUSE_PASSWORD: bm9yZHNwb3Q=

  # echo -n "your-entsoe-api-key" | base64
  ENTSOE_API_KEY: eW91ci1lbnRzb2UtYXBpLWtleQ==

  # echo -n "your-nordspot-api-key" | base64  (used for FastAPI auth)
  NORDSPOT_API_KEY: eW91ci1ub3Jkc3BvdC1hcGkta2V5
